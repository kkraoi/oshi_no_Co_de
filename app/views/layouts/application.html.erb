<!DOCTYPE html>
<html>
  <head>
    <title>推しのコード<%= "｜#{content_for(:title)}" if content_for?(:title) %></title>
    <%= render 'layouts/common/meta' %>
    <%= yield(:head) %>
  </head>

  <body>
    <%= yield %>
  </body>

  <%= render 'layouts/common/footer' %>
</html>
